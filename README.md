# react-game

**Technologies:**
- React
- Redux, Redux-Thunk
- TypeScript
- SCSS

**Setup:**

Clone this repo and run the following commands:
```bash
  cd react-game
  # to switch branch
  git switch react-game
  # to install packages
  npm install
  # to run website
  npm run start
```
